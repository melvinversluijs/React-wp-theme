import React from "react";

// Bring in components.
import Logo from "./Logo";
import Navbar from "./Navbar";

const Header = () => {
  return (
    <header>
      <Logo />
      <Navbar />
    </header>
  );
};

export default Header;
